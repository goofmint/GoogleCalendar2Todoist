/**
 * Apps Script の公開関数。
 * 実ロジックは Task 2.9 で実装する。
 */

export function sync(): void {
  // 時間トリガーから呼ばれる。ロック取得と全体の同期処理は Task 2.9 で実装する。
}

export function setup(): void {
  // 手動で 1 回実行する。シートの作成とトリガーの登録は Task 2.9 で実装する。
}
